$("#new_product_box").html("<%= escape_javascript(image_tag @product.image_url(:thumb).to_s) %> <hr>");

// $("<%= escape_javascript(render('product', :product => @product)) %>").hide().appendTo("#first_product").fadeIn(1000);
$("#new_product_box").append("<%= escape_javascript(render('details_form', :@product => @product)) %>");


var $container = $('.products');
$container.imagesLoaded(function(){
	$container.masonry();
	$container.masonry('reload')
});

